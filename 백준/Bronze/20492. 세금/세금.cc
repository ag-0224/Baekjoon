#include <bits/stdc++.h>

using namespace std;

int n;

int main() {

    cin >> n;

    cout << fixed << setprecision(0) << n * 0.78;

    cout << " "; 

    cout << fixed << setprecision(0) << n * 0.956;

    return 0;

}